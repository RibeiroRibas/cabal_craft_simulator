import 'package:flutter/material.dart';

class ThemeColors {
  static const MaterialColor division = Colors.grey;
  static const Color gray = Colors.white24;
  static const Color red = Colors.redAccent;
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color primary = Color.fromRGBO(0, 255, 66, 1);
  static const cardColor = Color.fromRGBO(34, 34, 50, 1);
  static const backgroundColor = Color.fromRGBO(24, 24, 40, 1);
}

final Map<int, Color> shades = {50: const Color.fromRGBO(0, 255, 66, 1)};
